using System;

class timeloop {
    static void Main (string[] args) {
        int num = int.Parse (Console.ReadLine ());
        for (int x = 1; x <= num; x++)
            Console.WriteLine (x + " Abracadabra");
    }
}
